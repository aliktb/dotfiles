To run this compose file and create a Portainer container, run the following command from this directory:

```bash
docker-compose -f portainer-compose up -d
```

provided that the docker daemon is running
